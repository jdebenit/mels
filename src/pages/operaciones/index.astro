---
import Layout from "../../layouts/Layout.astro";
---

<Layout title="MELS // OPERACIONES">
    <div class="content-container">
        <h1>OPERACIONES</h1>
        <div class="divider"></div>
        <section id="historicas">
            <h2>Operaciones históricas</h2>
            <p>SUMARIO EJECUTIVO PENDIENTE.</p>
        </section>
        <section id="archivos">
            <h2>Archivos clasificados</h2>
            <p>NIVEL DE SEGURIDAD INSUFICIENTE.</p>
        </section>
        <section id="semillas">
            <h2>Semillas de aventura</h2>
            <p>GENERANDO ESCENARIOS...</p>
        </section>
    </div>

    <style>
        .content-container {
            max-width: 800px;
            margin: 100px auto 0;
            padding: 2rem;
            color: #ccc;
        }
        h1 {
            color: var(--color-accent);
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 1rem;
        }
        h2 {
            color: var(--color-secondary);
            margin-top: 2rem;
            border-left: 3px solid var(--color-accent);
            padding-left: 1rem;
        }
        .divider {
            height: 2px;
            background: var(--color-accent);
            width: 100%;
            margin-bottom: 2rem;
            box-shadow: 0 0 10px var(--color-accent);
        }
    </style>
</Layout>
